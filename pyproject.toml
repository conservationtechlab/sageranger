[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "sageranger"
version = "1.0.4"
description = ""
dependencies = [
"requests>=2.25.0",
"argparse",
"schedule",
"datetime",
"selenium",
"pandas"
]
requires-python = ">=3.12"
